#!/bin/bash
sudo apt update -y
sudo apt install -y curl
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs
npm install @aws-sdk/client-lambda @aws-sdk/credential-provider-node uuid express cors